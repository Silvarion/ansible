- hosts: ndb-cluster-mgm
  remote_user: ansible
  become: yes
  become_method: sudo
  connection: ssh
  gather_facts: yes
  roles: 
    - ndb-cluster-mgm
  tasks:
  - include ndb-cluster-software.yml