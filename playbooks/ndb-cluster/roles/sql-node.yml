- hosts: ndb-cluster-sql
  remote_user: ansible
  become: yes
  become_method: sudo
  connection: ssh
  gather_facts: yes
  roles: 
    - ndb-cluster-sql
  tasks:
  - include ndb-cluster-software.yml