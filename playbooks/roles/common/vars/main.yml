vars_prompt:
    - name: "username"
      prompt: "Username?"
    - name: "password"
      prompt: "Enter password"
      private: yes
      encrypt: "sha512_crypt"
      confirm: yes
      salt_size: 7