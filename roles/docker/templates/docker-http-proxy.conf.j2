[Service]
Environment="PROXY_URL={{ ansible_facts['env']['PROXY_URL'] }}"
Environment="http_proxy={{ ansible_facts['env']['http_proxy'] }}"
Environment="https_proxy={{ ansible_facts['env']['https_proxy'] }}"
Environment="ftp_proxy={{ ansible_facts['env']['ftp_proxy'] }}"
Environment="no_proxy=127.0.0.1,localhost"
Environment="HTTP_PROXY={{ ansible_facts['env']['HTTP_PROXY'] }}"
Environment="HTTPS_PROXY={{ ansible_facts['env']['HTTPS_PROXY'] }}"
Environment="FTP_PROXY={{ ansible_facts['env']['FTP_PROXY'] }}"
Environment="no_proxy=127.0.0.1,localhost"
